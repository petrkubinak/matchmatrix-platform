## Poznámka k názvu složky fronted

Složka `fronted` obsahuje frontend aplikaci MatchMatrix (web UI).

Název vznikl historicky překlepem při zakládání projektu.
Kvůli existujícím závislostem v konfiguracích (Docker, skripty, build pipeline)
zůstává název zachován.

fronted = frontend